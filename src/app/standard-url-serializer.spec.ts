import { StandardUrlSerializer } from './standard-url-serializer';

describe('StandardUrlSerializer', () => {
  it('should create an instance', () => {
    expect(new StandardUrlSerializer()).toBeTruthy();
  });
});
