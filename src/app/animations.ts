import { trigger, state, style, animate, transition, query, keyframes } from '@angular/animations';


export const pageSlideIn = 
  trigger('slideInAnimation', [
    transition("customerRegister => customerLogin", [

    ])
  ]);
